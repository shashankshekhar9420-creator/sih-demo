# Both integration URLs and the server-only key have these defaults.
COMMERCE_APP_URL=http://localhost:3000
ARTISAN_APP_URL=http://localhost:4000
DEMO_API_KEY=local-demo-key
# Seller identity is fixed to seller-demo by the shared publish contract.
# SQLite defaults to prisma/artisan.db without any environment variables.
